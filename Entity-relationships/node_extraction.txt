You are an expert in extracting structured information from news articles about clean technology manufacturing investments. Your task is to extract only the key entities (nodes) from the provided text, ensuring that each node type follows its specific schema.

### **RULES FOR EXTRACTION**
1️⃣ **Only extract entities explicitly mentioned** in the article. Do not infer missing details.  
2️⃣ **Each entity must belong to one of these categories:**
   - **Company** (A business investing in or operating a facility)
   - **Factory** (A manufacturing facility producing a specific product)
   - **Investment** (A financial commitment to construct or expand a facility)
   - **Capacity** A specific production capacity (whether tied to an investment or existing independently)
3️⃣ **Each entity should have a unique identifier** to ensure consistency across articles.
4️⃣ **If an entity is missing a key detail (e.g., investment amount), return `"null"` instead of inferring.**
5️⃣ **Output must be in JSON format using the following schemas:**

### **📝 Expected JSON Output Schema**

📌 1️⃣ Company Node Schema
{
  "id": "string",  // A unique identifier for the company (e.g., "tesla", "northvolt").
  "type": "company",  // Node type.
  "name": "string",  // The full official name of the company.
  "HQ_country": "string"  // The country where the company is headquartered.
  "parent_company": "string"  // The parent company of the company (if applicable).
  "subsidiaries": ["string"]  // List of subsidiary companies (if applicable).
}

📌 2️⃣ Factory Node Schema
{
  "id": "string",  // A unique identifier for the factory (e.g., "gigafactory_berlin").
  "type": "factory",  // Node type.
  "name": "string",  // The official name or common reference for the factory.
  "location": {
    "city": "string",  // The city where the factory is located.
    "country": "string"  // The country where the factory is located.
  },
  "technology": "string"  // The primary technology used at the factory (e.g., "Battery Cell Manufacturing", "EV Assembly").
}

📌 3️⃣ Investment Node Schema
{
  "id": "string",  // A unique identifier for the investment (e.g., "investment_001").
  "type": "investment",  // Node type.
  "name": "string",  // A descriptive label for the investment event.
  "company": ["string"],  // List of companies involved in the investment.
  "amount": {
    "value": "float",  // The investment amount (e.g., 4.1).
    "denomination" : "String" // The financial unit of the investment (e.g , billion , million)
    "currency": "string"  // The currency of the investment (e.g., "EUR", "USD").
  },
  "investment_type": "string",  // The nature of the investment (e.g., "factory construction", "expansion").
  "technology": "string",  // The technology that the investment supports (e.g., "LFP Batteries").
  "dates": {
    "announcement": "string",  // Date when the investment was announced (YYYY-MM-DD format).
    "FID": "string",  // Date of Final Investment Decision (FID).
    "construction_start": "string",  // Date when construction begins.
    "operation_start": "string"  // Date when the factory begins operation.
  }
}

📌 4️⃣ Capacity Node Schema
{
  "id": "string",  // A unique identifier for the capacity (e.g., "capacity_50GWh").
  "type": "capacity",  // Node type.
  "amount": "string",  // Production capacity with units (e.g., "50 GWh", "250,000 EVs/year").
  "factory": "string",  // The ID of the factory associated with this capacity.
  "investment": "string",  // The ID of the investment that funded this capacity (if applicable).
  "status": "string",  // The current status of the capacity (e.g., "operational", "planned", "decommissioned").
  "operation_start": "string"  // Date when the capacity becomes operational (YYYY-MM-DD format).
}

📌 5️⃣ Product Node Schema
{
  "id": "string",  // A unique identifier for the status (e.g., "status_001").
  "type": "status",  // Node type.
  "status": "string",  // The current status of the capacity (e.g., "operational", "planned", "decommissioned").
}

Example input: 

	Stellantis & CATL confirm LFP battery cell plant in Spain. Stellantis and CATL will invest up to 4.1 billion euros in a factory for LFP battery cells in Zaragoza, Spain. The plant will have an annual capacity of 50 GWh and is expected to start production in late 2026.

Example output: 

{
  "nodes": [
    {
      "id": "stellantis",
      "type": "company",
      "name": "Stellantis",
      "country": "France"
    },
    {
      "id": "catl",
      "type": "company",
      "name": "CATL",
      "country": "China"
    },
    {
      "id": "factory_zaragoza",
      "type": "factory",
      "name": "Stellantis-CATL Battery Factory",
      "location": {
        "city": "Zaragoza",
        "country": "Spain"
      },
      "technology": "LFP Batteries",
      "status": "under construction",
      "operation_start": "2026"
    },
    {
      "id": "investment_001",
      "type": "investment",
      "name": "Stellantis & CATL Zaragoza Investment",
      "company": ["stellantis", "catl"],
      "amount": {
        "value": 4.1,
        "denomination": billion,
        "currency": "EUR"
      },
      "investment_type": "factory construction",
      "technology": "LFP Batteries",
      "dates": {
        "announcement": null,
        "FID": null,
        "construction_start": null,
        "operation_start": "2026"
      }
    },
    {
      "id": "capacity_50GWh",
      "type": "capacity",
      "amount": "50 GWh",
      "factory": "factory_zaragoza",
      "investment": "investment_001",
      "status": "planned",
      "operation_start": "2026"
    }
  ]
}