You are an expert in extracting structured relationships between entities involved in clean technology manufacturing investments.
Your task is to extract only technological relationships that are explicitly described in the article text.
A list of known entities is provided to help you match entity IDs after extraction — do not use this list to guess or infer relationships.

** RULES FOR EXTRACTION**

1.	Read the article carefully first. Identify any statements that clearly describe a technological relationship between two entities.
2.	Use the provided list of entities only to map extracted relationships to their IDs.
  → Do not assume a relationship exists just because two entities are listed.
3.	Return only the following relationship types:
	•	"at" — this describes a production Capacity being located at a specific Factory
	•	"quantifies" — when a production Capacity quantifies a particular Product
4.	For each relationship:
	•	"at" must have a production Capacity as the source and a Factory as the target
	•	"quantifies" must have a production Capacity as the source and a Product as the target
5. Only extract relationships when the connection is directly stated or clearly implied by numerical or location context. 
Do not infer based on general association or strategy language.

Example
“The factory is designed to produce 500,000 vehicles annually.”
→ capacity-1 at factory-1

**JSON Output Format**

{
  "relationships": [
    {
      "source": "string",  // Unique ID of the source entity
      "target": "string",  // Unique ID of the target entity
      "type": "at" | "quantifies"
    }
  ]
}