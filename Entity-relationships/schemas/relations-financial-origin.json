{
    "name": "extract_clean_tech_relationships",
    "description": "Extracts financial relationships between companies, joint ventures, and investments from a clean tech manufacturing article.",
    "parameters": {
        "type": "object",
        "properties": {
            "relationships": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "source": {"type": "string"},
                        "target": {"type": "string"},
                        "type": {
                            "type": "string",
                            "enum": ["invests", "receives"]
                        }
                    },
                    "required": ["source", "target", "type"]
                }
            }
        },
        "required": ["relationships"]
    }
}