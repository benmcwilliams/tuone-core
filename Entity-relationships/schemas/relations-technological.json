{
    "name": "extract_clean_tech_relationships",
    "description": "Extracts technological relationships between factories, products, and capacities from a clean tech manufacturing article.",
    "parameters": {
        "type": "object",
        "properties": {
            "relationships": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "source": {"type": "string"},
                        "target": {"type": "string"},
                        "type": {
                            "type": "string",
                            "enum": ["at", "quantifies"]
                        }
                    },
                    "required": ["source", "target", "type"]
                }
            }
        },
        "required": ["relationships"]
    }
}