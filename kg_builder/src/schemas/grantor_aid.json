{
    "name": "extract_clean_tech_relationships",
    "description": "Extracts relationships between grantors, aid, product and facilities from a clean tech manufacturing article.",
    "parameters": {
        "type": "object",
        "properties": {
            "relationships": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "source": {"type": "string"},
                        "target": {"type": "string"},
                        "type": {
                            "type": "string",
                            "enum": ["pays", "approves", "issues", "targets", "funds"]
                        }
                    },
                    "required": ["source", "target", "type"]
                }
            }
        },
        "required": ["relationships"]
    }
}